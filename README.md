# 📱 Task Management Application
A **Flutter-based** Android task and reminder application that helps users manage tasks, track due dates and categories, and receive local reminders with customizable notification settings.

# 🚀 Developer Tips
- Use `flutter pub outdated` to check package updates
- Use `flutter analyze` to inspect code issues
- Use `flutter test` to run available unit and widget tests
